/* global Office */

/**
 * Teacher Assistant - Commands Module
 * Minimal commands file for ribbon button support.
 * All logic has moved to taskpane.js (taskpane-only architecture).
 */

Office.onReady(() => {
    console.log('Commands.js: Office is ready');
});
